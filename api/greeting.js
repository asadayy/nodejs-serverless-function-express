const obj = {
  message: "I am Asad Bukhari!"
};

// To use it:
console.log(obj.message); // Output: "I am Asad Bukhari!"

// Or to convert it to a JSON string:
const jsonString = JSON.stringify(obj);
console.log(jsonString);
